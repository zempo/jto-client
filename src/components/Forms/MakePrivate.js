import React from "react";

const MakePrivate = () => {
  return (
    <div>
      <h1>Are you sure you want to switch {"from public to private / private to public"}</h1>
    </div>
  );
};

export default MakePrivate;
